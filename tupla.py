# Tupla é uma lista imutável

cores = ('vermelho', 'azul', 'verde', 'azul',)

#cores[0] = 'roxo' #TypeError: 'tuple' object does not support item assignment

# Métodos disponíveis para tupla:

# Count, Index e Len